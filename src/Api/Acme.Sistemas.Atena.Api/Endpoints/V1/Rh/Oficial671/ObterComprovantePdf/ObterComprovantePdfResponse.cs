namespace Acme.Sistemas.Atena.Api.Endpoints.V1.Rh.Oficial671.ObterComprovantePdf;

// Placeholder — o endpoint devolve bytes do PDF direto via Results.File, não JSON.
public sealed record ObterComprovantePdfResponse(string FileName, long Tamanho);
